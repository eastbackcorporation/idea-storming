<table class="index_table">
  <thead>
    <tr>
      <th>カテゴリ名</th>
      <th>並び順</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% nested_categories(Category.all.arrange(order: :disp_order)).each do |category| %>
      <tr>
        <td>
          <%= category[0] %>
        </td>

        <td>
          <%= category[2] %>
        </td>

        <td>
          <%= link_to '編集', edit_admin_category_path(category[1]), class: 'btn btn-danger' %>
          <%= link_to '子カテゴリ作成', new_admin_category_path(parent_category_id: category[1]), class: 'btn btn-danger' %>
          <%= link_to t('helpers.links.destroy'), admin_category_path(category[1]), data: { confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
