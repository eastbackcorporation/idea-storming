<div class='row'>
  <div class='col-12'>
    <div class='sign-in'>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <section>
        <div class="container">
          <div class="row justify-content-md-center">
            <div class="card-wrapper boder-0 shadow-sm">
              <div class="card fat">
                <div class="card-body">
                  <h4 class="card-title"><%= t('devise.passwords.new.forgot_your_password') %></h4>
                  <%= render "users/shared/flash_messages" %>
                  <%= devise_error_messages! %>

                  <div class="form-group">
                    <%= f.label :email %>
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control'  %>
                  </div>
                  <p class="text-muted">
                    <small><%= t("devise.passwords.new.forgot_your_password_annotation") %></small>
                  </p>
                  <div class="form-group no-margin">
                    <%= f.submit t("devise.passwords.new.send_me_reset_password_instructions"), class: 'btn btn-success btn-block' %>
                  </div>
                  <div class="margin-top20 text-right">
                    <%= render "users/shared/links" %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    <% end %>
    </div>
  </div>
</div>
