<p id="notice"><%= notice %></p>






<% if user_signed_in? %>
  <%= current_user&.nick_name %>
  <%= link_to t("devise.shared.links.sign_out"), destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to t("devise.shared.links.sign_in"), new_session_path(User) %>/
  <%= link_to t("devise.shared.links.sign_up"), new_registration_path(User) %>
<% end %>

<hr/>
<strong><%= link_to "テーマ一覧" ,themes_path %></strong>

